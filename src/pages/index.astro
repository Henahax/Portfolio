---
import "@/styles/globals.css";
import Layout from "../layouts/Layout.astro";
import { Badge } from "@/components/ui/badge";
import Project from "@/components/Project.astro";
---

<script is:inline>
  const getThemePreference = () => {
    if (typeof localStorage !== "undefined" && localStorage.getItem("theme")) {
      return localStorage.getItem("theme");
    }
    return window.matchMedia("(prefers-color-scheme: dark)").matches
      ? "dark"
      : "light";
  };
  const isDark = getThemePreference() === "dark";
  document.documentElement.classList[isDark ? "add" : "remove"]("dark");

  if (typeof localStorage !== "undefined") {
    const observer = new MutationObserver(() => {
      const isDark = document.documentElement.classList.contains("dark");
      localStorage.setItem("theme", isDark ? "dark" : "light");
    });
    observer.observe(document.documentElement, {
      attributes: true,
      attributeFilter: ["class"],
    });
  }
</script>

<Layout title="Welcome to Astro.">
  <main>
    <section>
      <div class="flex">
        <img
          class="rounded-full"
          src="https://avatars.githubusercontent.com/u/23476818"
          alt="henahax"
        />
        <div>
			<h2>Tobias Halupczok</h2>
			<p>dev</p>
		</div>
      </div>
    </section>
    <section>Timeline</section>
    <section>
      <div class="grid gap-4 p-4">
        <Project
          name="Myfactory"
          link="https://myfactory.com"
          type="professionell"
          description="Cloud ERP System"
        >
          <Fragment slot="technologies">
            <Badge variant="outline">C#</Badge>
            <Badge variant="outline">.NET</Badge>
            <Badge variant="outline">
              <span class="flex flex-row gap-1.5 items-center">
                <i class="fa-brands fa-js"></i>JavaScript
              </span>
            </Badge>
          </Fragment>
        </Project>
        <Project
          name="HomeLab"
          link="https://github.com/Henahax/HomeLab"
          type="privat"
          description="test"
        />
      </div>
    </section>
    <section>Contact</section>
  </main>
</Layout>

<style></style>
